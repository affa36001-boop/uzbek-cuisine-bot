# 🚀 Инструкция по деплою — Uzbek Cuisine Bot

## Архитектура деплоя

```
Telegram Bot  ←→  Backend (Railway)  ←→  SQLite БД
                       ↕
               Frontend (Vercel)
```

| Компонент | Платформа | Стоимость |
|-----------|-----------|-----------|
| Бэкенд (Node.js + SQLite) | **Railway** | Бесплатно |
| Фронтенд (React) | **Vercel** | Бесплатно |
| Домен (HTTPS) | Автоматически | Бесплатно |

---

## Шаг 0 — Подготовка (один раз)

### 0.1 Установи Git (если нет)
Скачай с https://git-scm.com и установи.

### 0.2 Создай аккаунты
- **GitHub**: https://github.com/signup
- **Railway**: https://railway.app (войди через GitHub)
- **Vercel**: https://vercel.com/signup (войди через GitHub)

---

## Шаг 1 — Загрузи проект на GitHub

Открой **PowerShell** в папке `uzbek_cuisine_bot` и выполняй команды по одной:

```powershell
# Входим в папку проекта
cd "C:\Users\toshk\OneDrive\Desktop\uzbek_cuisine_bot"

# Инициализируем git
git init

# Добавляем все файлы
git add .

# Первый коммит
git commit -m "Initial commit"
```

Затем на GitHub:
1. Зайди на https://github.com/new
2. Название репозитория: `uzbek-cuisine-bot`  
3. Сделай **Private** (приватный) — чтобы секреты не утекли
4. Нажми **Create repository**
5. Скопируй URL репозитория (вида `https://github.com/ВАШ_НИК/uzbek-cuisine-bot.git`)

Вернись в PowerShell:
```powershell
# Подключаем к GitHub (замени URL на свой)
git remote add origin https://github.com/ВАШ_НИК/uzbek-cuisine-bot.git

# Загружаем
git push -u origin main
```

---

## Шаг 2 — Деплой бэкенда на Railway

### 2.1 Создай проект
1. Зайди на https://railway.app
2. Нажми **New Project**
3. Выбери **Deploy from GitHub repo**
4. Выбери `uzbek-cuisine-bot`
5. Railway спросит какую папку — укажи: **`backend`**

### 2.2 Настрой переменные окружения
В Railway нажми на сервис → вкладка **Variables** → добавь каждую строку:

```
BOT_TOKEN          = 123456789:AABBccDDee...   (от @BotFather)
ADMIN_TELEGRAM_ID  = 8083248063               (твой Telegram ID)
ADMIN_PASSWORD     = ПридумайСложныйПароль123
NODE_ENV           = production
DATABASE_PATH      = ./database.sqlite
```

> ⚠️ `WEBAPP_URL` и `ALLOWED_ORIGINS` добавишь **после** деплоя фронтенда

### 2.3 Дождись деплоя
Railway сам установит зависимости и запустит сервер.  
В разделе **Deployments** ты увидишь статус — дождись зелёного ✅.

### 2.4 Получи URL бэкенда
Вкладка **Settings** → **Domains** → нажми **Generate Domain**.  
Скопируй URL вида: `https://uzbek-cuisine-backend-xxxx.up.railway.app`

---

## Шаг 3 — Деплой фронтенда на Vercel

### 3.1 Создай проект
1. Зайди на https://vercel.com
2. Нажми **Add New → Project**
3. Выбери репозиторий `uzbek-cuisine-bot`
4. **Root Directory** → укажи `frontend`

### 3.2 Настрой переменные окружения
В Vercel перед деплоем нажми **Environment Variables** и добавь:

```
VITE_API_URL = https://uzbek-cuisine-backend-xxxx.up.railway.app
```
(URL бэкенда с Railway из шага 2.4)

### 3.3 Настройки сборки (должны определиться автоматически)
```
Framework Preset : Vite
Build Command    : npm run build
Output Directory : dist
Install Command  : npm install
```

### 3.4 Нажми Deploy
Подожди 1-2 минуты. Получишь URL вида: `https://uzbek-cuisine-bot.vercel.app`

---

## Шаг 4 — Обновить бэкенд URL фронтенда

Вернись в Railway → Variables → добавь/обнови:
```
WEBAPP_URL       = https://uzbek-cuisine-bot.vercel.app
ALLOWED_ORIGINS  = https://uzbek-cuisine-bot.vercel.app
```

После добавления Railway автоматически перезапустит сервер.

---

## Шаг 5 — Настрой Telegram бота

### 5.1 Зарегистрируй WebApp URL
1. Открой Telegram → найди **@BotFather**
2. Напиши `/mybots` → выбери своего бота
3. **Bot Settings → Menu Button → Configure menu button**
4. Укажи URL: `https://uzbek-cuisine-bot.vercel.app`

### 5.2 Проверь бота
1. Найди своего бота в Telegram
2. Нажми **START**
3. Нажми кнопку **🍽️ Меню**
4. Оформи тестовый заказ
5. Убедись что пришло уведомление с кнопками статусов

---

## Шаг 6 — Проверь что всё работает

### Чеклист:
- [ ] Бэкенд доступен: `https://ВАШ_БЭКЕНД.up.railway.app/api/health`
  - Должен вернуть `{"status":"ok",...}`
- [ ] Фронтенд открывается: `https://ВАШ_ФРОНТЕНД.vercel.app`
- [ ] В Telegram бот отвечает на `/start`
- [ ] Меню открывается при нажатии кнопки
- [ ] Заказ проходит и приходит уведомление
- [ ] Кнопки статусов работают

---

## Обновление проекта после изменений в коде

```powershell
cd "C:\Users\toshk\OneDrive\Desktop\uzbek_cuisine_bot"

git add .
git commit -m "Описание что изменил"
git push
```
> Railway и Vercel автоматически подхватят изменения и перезапустятся ✨

---

## ❓ Частые проблемы

### Бэкенд не запускается (Railway)
- Проверь что все переменные окружения заданы
- Посмотри логи: вкладка **Deployments → View Logs**

### Фронтенд показывает пустую страницу
- Проверь что `VITE_API_URL` задан правильно (без слэша в конце)
- Проверь `vercel.json` есть ли в папке frontend

### Заказы не приходят в Telegram
- Убедись что `BOT_TOKEN` правильный
- `ADMIN_TELEGRAM_ID` — проверь свой ID через @userinfobot

### CORS ошибки
- Проверь что `ALLOWED_ORIGINS` содержит точный URL фронтенда
