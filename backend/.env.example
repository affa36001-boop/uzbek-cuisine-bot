# ─────────────────────────────────────────
#  Uzbek Cuisine Bot — Backend .env
#  Скопируй этот файл в .env и заполни
# ─────────────────────────────────────────

# Telegram Bot (получить у @BotFather)
BOT_TOKEN=your_bot_token_here

# Твой Telegram ID (узнать у @userinfobot)
ADMIN_TELEGRAM_ID=8083248063

# Пароль для Admin API (придумай сам, любая строка)
ADMIN_PASSWORD=your_secret_admin_password

# URL фронтенда (после деплоя на Vercel/Netlify)
WEBAPP_URL=https://your-app.vercel.app

# URL бэкенда (после деплоя на Railway/Render)
# Используется для CORS
ALLOWED_ORIGINS=https://your-app.vercel.app

# Porт сервера (Railway задаёт автоматически, не трогать)
PORT=3002

# Окружение
NODE_ENV=production

# Путь к SQLite базе данных
DATABASE_PATH=./database.sqlite
